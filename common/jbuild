;; Shared bits between the syntax extension and the runtime library

(library (
  (name expect_test_common)
  (public_name ppx_expect.common)
  (public_release ((desc "Shared parts for ppx_expect")))
  (libraries (sexplib))
  (preprocess (((pps (ppx_compare
                      ppx_sexp_conv
                      )) All)))
  ))
