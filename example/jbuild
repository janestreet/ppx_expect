
(library (
  (name expect_test_examples)
  (libraries (core async))
))

;; hydra doesn't like .ml files containing tab chars
(rule ((targets (tabs.ml)) (deps (tabs.ml.in)) (action "cp ${^} ${@}")))

(alias ((name DEFAULT) (deps (tests.ml.pp))))
